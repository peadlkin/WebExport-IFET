name = "ifet-feedback"
main = "src/index.js"
compatibility_date = "2025-12-20"

# Deploy as: https://webexport.pro/api/ifet/feedback
# (Configure a route in Cloudflare: webexport.pro/api/ifet/feedback*)

[vars]
# Set allowed origin for CORS (optional). Example:
# ALLOWED_ORIGIN = "https://webexport.pro"
ALLOWED_ORIGIN = "https://webexport.pro"



